/**
 * Hooks index - Export all custom hooks for easy importing
 */

export { useDebounce } from './useDebounce';
export { useSchemaData, useComputedData } from './useSchemaData';
export { useSimpleData } from './useSimpleData';
export { 
  usePrototype, 
  usePrototypeQuery, 
  usePrototypeCreate, 
  usePrototypeSearch,
  usePrototypeStats
} from './usePrototype';